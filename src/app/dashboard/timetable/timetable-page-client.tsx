'use client'

import type { UserSubjectWithSubject } from '@/lib/types/database'

interface TimetablePageClientProps {
  userId: string
  userName: string
  initialSubjects: UserSubjectWithSubject[]
}

export function TimetablePageClient({}: TimetablePageClientProps) {
  return null
}
